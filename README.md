Micro service secret manager

```
git clone git@github.com:feed-manager-bdx/snapfeat-login-platform.git
cd .../snapfeat-login-plaform
docker-compose build
docker-compose up
```

Puis dans le conteneur serveur web

```
composer install
```


